# Two-minute narrated demo, September 8

The owner requested a **two-minute** demo video with OpenAI voice. The local deliverable is `data/demo-video-20260908/final/secops-triage-demo-2min.mp4`, with burned-in captions and `secops-triage-demo.srt`. This replaces the earlier five-minute production target; five minutes remains the hackathon's maximum. The final bundle includes a poster and representative frames. It has not been uploaded to YouTube/Vimeo or submitted to Devpost.

The 1920×1080 H.264/AAC video shows an actual recorded local scripted Strands investigation: select the phishing incident, inspect four scoped tools and nine reads, compare the two message scopes, open the exact-follow-up intelligence citation, preserve uncertainty and save an explicitly automated demonstration handoff. There is one SDK session, one local handoff and no final human review. The app runs from verified commit `c97868c809ce4f753ed05ee76a9b64ca303bb4dc`. No response fixtures were used for this recording. The application source was not changed to create the video.

The architecture graphic distinguishes the Strands SDK, scripted provider, local host controls and the separately verified Ohio EC2/EBS/SSM/CloudWatch deployment. The AWS host remains stopped; this UI has not been deployed there. Bedrock, AgentCore and live security connectors remain unimplemented. The closing frame explicitly states that scripted findings fail the completeness rubric and live-model/human validation is still pending.

## Voice and production evidence

Eight bounded OpenAI speech requests completed once, with no automatic retries: `gpt-4o-mini-tts-2025-12-15`, built-in voice `cedar`, 113.55 seconds of original audio. The owner's explicit video-with-voice request authorized this narration; it did not reopen investigation grants or authorize AWS actions. Each request has a durable exclusive intent and result record in the private build directory. Credentials are never printed or embedded in media. No investigation model call, paid transcription call, campaign change or AWS action occurred.

[Official OpenAI speech guidance](https://developers.openai.com/api/docs/guides/text-to-speech) informed the endpoint/voice selection and AI-voice disclosure. [Model pricing](https://developers.openai.com/api/docs/models/gpt-4o-mini-tts), checked September 8, lists $0.60 per million text-input tokens and $12 per million audio-output tokens. Binary responses did not return token usage; exact billing is not claimed.

The original requested screenplay is `ops/demo_video/story.json`. Local speech recognition found that the generated scene04 audio omitted its final sentence, “That approval does not transfer.” Final captions omit the unspoken sentence via `caption_overrides.json`; the scope limit remains explicit on screen and in the preceding explanation. Original input, audio, first cut and correction evidence are retained. Caption timing is approximate by phrase, not certified word alignment.

Independent AI review corrected source attribution, human-validation wording and paid-dispatch persistence/validation before production. Final artwork, recording metadata and media code were independently reviewed. Representative final frames, including the authorization panel and the exact citation, were inspected by the primary agent. The full video decoded without errors. First-cut narration measured −16.34 LUFS and −4.30 dBTP, with the same audio retained in the final cut. Local CPU speech recognition recovered the main narration and disclosures; this is not a human listening or usability review.

All 71 previously frozen application/test/assets/config files still match their verified hashes. The prior 310-test and 14-browser-check evidence remains application verification, not a new live-evaluation or media acceptance claim. Media source hashes were unchanged through final rendering. See `outputs/secops-demo-video-validation-20260908.json` for the final media hash, codec/duration, capture identity and limitations. The container is approximately 120 seconds; AAC packet padding adds a few milliseconds.

## Reproduction and next action

`ops/demo_video/capture.cjs` records into a new private workspace using existing Playwright and Chrome. `artwork.py` and `render.py` use Pillow and FFmpeg to build the graphics and video. `narrate.py` is a paid speech producer, separate from all investigation authority paths; do not invoke it automatically on resume. Its existing directory refusal prevents replay into a consumed batch. Reuse the preserved audio for ordinary editing.

The existing media bundle is local and ignored by Git. Source, screenplay and sanitized verification are committed; no private capabilities or raw stores are published. Review the video, then arrange the owner-approved public upload and final Devpost submission. License selection, submission architecture attachment, remaining live evaluation and human validation remain separate unfinished work. Rules rechecked September 8 at https://agentsforhumans.devpost.com/rules: public YouTube/Vimeo video, public MIT/Apache source/setup/assets, diagram and AWS Builder ID required; deadline September 14 at 8pm Eastern. Nothing in this video production records human acceptance or authorizes submission.

## Owner-requested video revision — September 8

The latest deliverable is `data/demo-video-20260908/final-v2/secops-triage-demo-2min.mp4`: the closing slide and OpenAI Cedar voice now emphasize “From alert to evidence-backed analyst handoff.” All burned-in captions are removed; no SRT accompanies this cut. Recorded/scripted/synthetic and AI-voice labels remain. Internal failed-evaluation evidence remains unchanged in project records; it is omitted from the revised closing presentation. Original media and validation are preserved.

One replacement closing speech request completed, with no paid retry. Two local import-path failures occurred before dispatch and their batch manifests remain preserved; the successful invocation used `PYTHONPATH=.`. Seven original audio segments and every scene duration are unchanged. The new closing fits without truncation. Offline ASR recovered its intended text; full video decoding and representative closing/evidence frame inspection passed. No human acceptance, live investigation inference, AWS action or public upload occurred. Frozen media sources matched after rendering; application source remains unchanged from the verified baseline. See `outputs/secops-demo-video-revision-20260908.json`. Renderer defaults to no captions; `--captions` opts into the original caption mode and `--timing` preserves an existing timeline.
