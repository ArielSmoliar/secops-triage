[project]
name = "secops-triage"
version = "0.1.0"
description = "Evidence-backed investigation of existing SIEM incidents"
requires-python = ">=3.11"

[project.optional-dependencies]
agent = ["strands-agents==1.54.0", "certifi==2026.7.22"]

[tool.pytest.ini_options]
testpaths = ["tests"]

